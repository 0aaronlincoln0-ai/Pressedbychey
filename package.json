{
  "name": "pressedbychey-site",
  "private": true,
  "scripts": {
    "check": "node --check script.js && node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
